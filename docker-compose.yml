version: '2'
services:
  graphdb:
    container_name: graph-db-instance
    image: khaller/graphdb-free
    restart: always
    volumes:
      - ~/data/graphdb-data:/opt/graphdb/data
    ports:
      - '80:7200'
    command: -Dgraphdb.workbench.cors.enable=true -Xms4G -Xmx8G
